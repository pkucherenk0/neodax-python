{
  "name": "nimbus-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Visual e2e for nimbus-python (mock wallet, no real MetaMask extension) -- the UI-e2e layer, paired with suites/ (API integration). See ../AGENTS.md for why this is a separate Node project.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@johanneskares/wallet-mock": "^1.4.1",
    "@playwright/test": "^1.56.0",
    "dotenv": "^17.4.2",
    "typescript": "^5.6.0",
    "viem": "^2.23.14"
  }
}
