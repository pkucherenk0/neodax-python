[pytest]
testpaths = tests
python_files = test_*.py
timeout = 180
timeout_func_only = true
; mirrors the old e2e/playwright.config.ts's `use` block:
addopts = --browser chromium --screenshot only-on-failure --tracing retain-on-failure
